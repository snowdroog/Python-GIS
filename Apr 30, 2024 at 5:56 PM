WITH RECURSIVE DateRanges AS (
  SELECT CURRENT_DATE AS Date
  UNION ALL
  SELECT Date + INTERVAL '1 day'
  FROM DateRanges
  WHERE Date < CURRENT_DATE + INTERVAL '6 months'
)
SELECT DR.Date, R.Region
FROM DateRanges DR
CROSS JOIN (SELECT DISTINCT Region FROM YOUR_TABLE) R